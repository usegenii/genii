/** Maximum timeout allowed for shell commands (5 minutes) */
export const MAX_TIMEOUT_MS = 300_000;

/** Default timeout for shell commands (30 seconds) */
export const DEFAULT_TIMEOUT_MS = 30_000;

/** Default maximum output length in characters */
export const DEFAULT_MAX_OUTPUT_LENGTH = 50_000;

/** Indicator prepended to truncated output */
export const TRUNCATION_INDICATOR = '[...output truncated...]\n';
