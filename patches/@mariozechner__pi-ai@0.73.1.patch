diff --git a/dist/providers/google-shared.js b/dist/providers/google-shared.js
--- a/dist/providers/google-shared.js
+++ b/dist/providers/google-shared.js
@@ -55,7 +55,9 @@ function resolveThoughtSignature(isSameProviderAndModel, signature) {
  * Models via Google APIs that require explicit tool call IDs in function calls/responses.
  */
 export function requiresToolCallId(modelId) {
-    return modelId.startsWith("claude-") || modelId.startsWith("gpt-oss-");
+    const geminiMajorVersion = getGeminiMajorVersion(modelId);
+    return (geminiMajorVersion !== undefined && geminiMajorVersion >= 3) ||
+        modelId.startsWith("claude-") || modelId.startsWith("gpt-oss-");
 }
 function getGeminiMajorVersion(modelId) {
     const match = modelId.toLowerCase().match(/^gemini(?:-live)?-(\d+)/);
